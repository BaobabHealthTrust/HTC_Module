<%= form_for(@htc) do |f| %>
  <% if @htc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@htc.errors.count, "error") %> prohibited this htc from being saved:</h2>

      <ul>
      <% @htc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
