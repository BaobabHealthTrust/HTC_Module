<html>
	<head>
		<script>
				<% if defined? params[:client_id] %>
						var cancelDestination = "clients/<%= params[:client_id] %>";
				<% else %>
						var cancelDestination = "htcs";
				<% end %>
		</script>
	</head>
<body>
			<%= form_tag({:action => "set_date"}, {:method => "post"}) do %>
					<% if defined? params[:client_id] %>
						<input type="hidden" value="<%= params[:client_id] %>" name="client_id"/>
					<% end %>
				<br />
				<fieldset>
					<legend>Set Date</legend>
						<label for="date">Date</label>
						<input type="text" name="date" id="date" fieldtype="date">
						<br />
				</fiedset>
				<%= link_to "Cancel" , protocols_path %>
				<%= submit_tag "Finish" %>
					
			<%end%>
		 <%= javascript_include_tag "ts" %>
</body>
</html>
